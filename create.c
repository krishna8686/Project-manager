#include <stdio.h>
#include <string.h>
#include <yaml.h>
                                                                  // this code is for creating folder or projects

////////////////////////////////////////////////////////

void create5(char * a,char *b){

  FILE* file_ptr = fopen(FILE_NAME, "w");

                                                                  ///this code is for creating A type folder or in B type projects



  fclose(file_ptr);
  
    
}
/////////////////////////////////////////////////////////////////////////
void create3(char *b){

    FILE* file_ptr = fopen(FILE_NAME, "w");
                                                                   ///his code is for creating B type projects in default type structures..




  fclose(file_ptr);

    
}

